diff --git a/beckend/app/routes/upload.py b/beckend/app/routes/upload.py
index 8f44042..4d8f19f 100644
--- a/beckend/app/routes/upload.py
+++ b/beckend/app/routes/upload.py
@@ -27,6 +27,10 @@ async def upload_document(
     file: UploadFile = File(...),
     lang: str = Query(DEFAULT_LANG),
 ):
+    # Normalize language string (e.g., "eng hin" -> "eng+hin", "eng,hin" -> "eng+hin")
+    if lang:
+        lang = lang.replace(" ", "+").replace(",", "+")
+
     original_name = file.filename
     ext = Path(file.filename).suffix or ".bin"
     uid = uuid.uuid4().hex
diff --git a/frontend/app/upload/page.tsx b/frontend/app/upload/page.tsx
index f3036a9..c92f60a 100644
--- a/frontend/app/upload/page.tsx
+++ b/frontend/app/upload/page.tsx
@@ -42,7 +42,7 @@ export default function OCRUploadPage() {
     setCurrentPage(0);
 
     try {
-      const res = await fetchWithAuth(api(`/upload?lang=${lang}`), {
+      const res = await fetchWithAuth(api(`/upload?lang=${encodeURIComponent(lang)}`), {
         method: "POST",
         body: formData,
       });
